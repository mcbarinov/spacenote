import React from "react"
import { Stack, Group, Title, Text, Badge, Paper, Divider, Card, Image } from "@mantine/core"
import { CustomLink } from "@spacenote/common/components"

/** Mantine components available in playground scope */
export const mantineScope = { React, Stack, Group, Title, Text, Badge, Paper, Divider, Card, Image, CustomLink }
